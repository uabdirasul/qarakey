import Use from "@/components/Use";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Maketlerden paydalanıw",
  description:
    "Qaraqalpaq tilindegi klaviatura maketlerden paydalanıw instrukciyası"
};

const page = () => {
  return <Use />;
};

export default page;
